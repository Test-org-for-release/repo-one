name: Build and Release Binaries

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  build:
    name: Build binaries
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          # see https://docs.github.com/en/actions/reference/runners/github-hosted-runners
          - os: ubuntu-latest
            suffix: linux-x86_64
          - os: macos-14
            suffix: macos-arm64

    steps:
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Install Task
        uses: go-task/setup-task@v1
        with:
          version: '3.x'

      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Determine version
        id: version
        run: echo "version=${{ github.ref_name }}" >> $GITHUB_OUTPUT

      - name: Run CI task from Taskfile
        run: task ci

      - name: Build binary
        run: |
          task build
          mkdir -p build
          cp bin/test-app build/

      - name: Create ZIP package
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          ARCHIVE="test-app-${VERSION}-${{ matrix.suffix }}.zip"
          cd build
          zip -r "../$ARCHIVE" test-app
        shell: bash

      - name: Upload artifact (ZIP)
        uses: actions/upload-artifact@v4
        with:
          name: test-app-${{ matrix.suffix }}
          path: test-app-${{ steps.version.outputs.version }}-${{ matrix.suffix }}.zip

  release:
    name: Upload artifacts to release
    needs: build
    runs-on: ubuntu-latest

    steps:
      # Download all build artifacts produced in the previous job
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      # Upload artifacts to GitHub Release
      - name: Upload release assets
        uses: softprops/action-gh-release@v2
        with:
          files: dist/**/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
