name: Markdownlint

on:
  push:
    branches: [ main ]

jobs:
  markdownlint-pipeline:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v5

    - name: Run own MDL action
      uses: test-org-for-release/.github/actions/markdownlint@main
